@model CloasisWeb.Models.EmailViewModel

@{
    ViewBag.Title = "Email";
}




@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <ul>
                    <li><p> Subject:</p><input type="text" name="subject" /></li>
                    <li><p> Message:</p><input type="text" name="message" /></li>
                    <li><p></p><input type="submit" value="Send" class="btn btn-default" /></li>
                </ul>
                
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

